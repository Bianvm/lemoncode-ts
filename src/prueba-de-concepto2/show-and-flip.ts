/*const backCard = document.getElementById("backCardContainer");
const LION_IMAGE =
  "https://raw.githubusercontent.com/Lemoncode/fotos-ejemplos/main/memo/1.png";

function changeToAnimalImg() {
  if (
    backCard !== null &&
    backCard !== undefined &&
    backCard instanceof HTMLElement
  ) {
    const img = backCard.getElementsByClassName("card__image").item(0);
    if (img !== null && img !== undefined && img instanceof HTMLImageElement) {
      img.src = img.src === LION_IMAGE ? "" : LION_IMAGE;
    }
  }
}

backCard?.addEventListener("click", changeToAnimalImg);*/
